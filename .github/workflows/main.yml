name: 02-workflow-events
on: 
 push:




jobs:
   echo:
      runs-on: ubuntu-latest
      steps:
          - name: show the trigger
            run: echo "I am trigerred by an ${{github.event_name}}"
